# CyborgMind V2.6 Production Docker Compose

version: '3.8'

services:
  cyborgmind:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: cyborgmind:v2.6
    container_name: cyborgmind-brain
    restart: unless-stopped
    
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - LOG_LEVEL=INFO
    
    volumes:
      - ./checkpoints:/app/checkpoints
      - ./logs:/app/logs
      - ./data:/app/data
    
    ports:
      - "8000:8000"
      - "9090:9090"
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    
    networks:
      - cyborgmind-network

  prometheus:
    image: prom/prometheus:latest
    container_name: cyborgmind-prometheus
    restart: unless-stopped
    ports:
      - "9091:9090"
    networks:
      - cyborgmind-network

  grafana:
    image: grafana/grafana:latest
    container_name: cyborgmind-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    ports:
      - "3000:3000"
    networks:
      - cyborgmind-network

networks:
  cyborgmind-network:
    driver: bridge
