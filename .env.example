# CyborgMind API Configuration
AUTH_TOKEN=cyborg-secret-v2
JWT_ENABLED=false
JWT_SECRET=
DEVICE=cpu

# Grafana Configuration
# Set strong, unique credentials for Grafana. Do NOT use 'admin'/'admin'.
GRAFANA_USER=
GRAFANA_PASSWORD=
