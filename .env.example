# CyborgMind API Configuration
# SECURITY: These values MUST be changed before deployment

# API Authentication (REQUIRED - generate with: openssl rand -hex 32)
AUTH_TOKEN=

# JWT Authentication (Optional, for production)
JWT_ENABLED=false
JWT_SECRET=  # Generate with: openssl rand -hex 64

# Device Configuration
DEVICE=cpu

# Grafana Configuration (if using monitoring stack)
# SECURITY: Set strong, unique credentials
GRAFANA_USER=
GRAFANA_PASSWORD=
